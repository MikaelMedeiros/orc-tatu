import { RemoveClassCheckboxDirective } from './remove-class-checkbox.directive';

describe('RemoveClassCheckboxDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoveClassCheckboxDirective();
    expect(directive).toBeTruthy();
  });
});
